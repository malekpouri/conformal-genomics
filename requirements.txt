# ConformalGen — runtime dependencies (design phase; pinned at implementation)
numpy>=1.26
scipy>=1.11          # quantiles, statistical tests
scikit-learn>=1.3    # quantile regression, domain classifier for weighted conformal
torch>=2.6           # property predictors (BF16); CUDA build recommended
matplotlib>=3.8      # coverage/width figures
